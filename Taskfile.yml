# https://taskfile.dev

version: "2"

tasks:
  dev:
    desc: set up development environment
    cmds:
      - go get ./...

  build:
    desc: Build locally
    cmds:
      - go build *.go

  run:
    desc: run off of source
    cmds:
      - go run *.go
